<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>{{#if pageTitle}}{{pageTitle}}{{else}}Page Title{{/if}}</title>

	{{#if html }}
	<!-- HTML IS TRUE -->
	{{/if}}
</head>
<body>
	{{> yield }}

	<!-- ENGINE CODE -->
	{{{engineData}}}
	<!-- END ENGINE CODE -->
</body>
</html>